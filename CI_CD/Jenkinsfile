def mygvscript
pipeline {
    agent any
    stages{
        stage('Hello') {
            steps {
                sh '''
                ls -l
                ansible --version
                ansible-playbook --version
                ansible-galaxy --version
                '''
            }
        }
    }
}
 